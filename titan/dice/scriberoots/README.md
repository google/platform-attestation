All trusted Titan v3 scribe root certificates. These are self-signed public
key root certificates for the keys which sign Titan DeviceID certificates.

These certificates use a custom binary format. See titancertutil for code which
parses and displays these certificates in a human-readable way.
